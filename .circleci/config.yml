# Use the latest 2.1 version of CircleCI pipeline process engine.
# See: https://circleci.com/docs/configuration-reference
version: 2.1

# Define a job to be invoked later in a workflow.
# See: https://circleci.com/docs/configuration-reference/#jobs
jobs:
  build:
    working_directory: ~/MyOnlineMarket
    # Specify the execution environment. You can specify an image from Dockerhub or use one of our Convenience Images from CircleCI's Developer Hub.
    # See: https://circleci.com/docs/configuration-reference/#docker-machine-macos-windows-executor
    docker:
      - image: circleci/golang:1.16
    # Add steps to the job
    # See: https://circleci.com/docs/configuration-reference/#steps
    steps:
      - checkout
      - setup_remote_docker:
          version: 20.10.7

      - run:
          name: Docker login
          command: |
            docker login -u moyuno1 -p kurumi9452

      - run:
          name: Create image
          command: |
            docker build -t 'us-central1-docker.pkg.dev/my-online-market-388401/hello-repo/my-online-market-app:circleci' .

      - run:
          name: AR login
          command: |
            cat secrets/my-online-market-388401-852b0214ee2c.json | docker login -u _json_key --password-stdin \
            https://us-central1-docker.pkg.dev

      - run:
          name: Upload image
          command: |
            docker push us-central1-docker.pkg.dev/my-online-market-388401/hello-repo/my-online-market-app:circleci

filters:
  paths:
    ignore:
      - "k8s/**"

# Invoke jobs via workflows
# See: https://circleci.com/docs/configuration-reference/#workflows
workflows:
  sample: # This is the name of the workflow, feel free to change it to better match your workflow.
    # Inside the workflow, you define the jobs you want to run.
    jobs:
      - build
